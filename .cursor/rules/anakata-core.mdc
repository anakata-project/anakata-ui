---
description: Anakata project core rules — applies to every repo (api, ui, rms, crm, engine)
alwaysApply: true
---

# Anakata — core rules

## What we are building
Three products for Anakata (Galápagos yacht expeditions, yachts ANAMARA and ANATIVA) that behave as one sales pipeline:
- **RMS** (reservations) — the ledger: inventory, departures, rates, offers, bookings, payments, commissions, documents, guest personal data, policies.
- **Booking Engine** (anakata.co) — the shop window: renders only what the RMS publishes, captures behaviour and requests.
- **CRM** — the relationship: contacts, identity, attribution, consent, lifecycle, segments, journeys, deals, tasks, conversations. **The CRM has no write path to money.**

**RMS and CRM are two sections of one app** (one Laravel API, one staff panel). The engine stays a separate public frontend.

Repos (all open in one Cursor multi-root workspace):

| Repo | What | Port |
|---|---|---|
| `anakata-api` | Laravel 13 + MySQL 8 + Redis. Conventional Laravel; route sections `/api/rms`, `/api/crm`, `/api/engine` | 8000 |
| `anakata-ui` | Shared Nuxt layer: design tokens, Nuxt UI theme, shared components, composables, API types | — |
| `anakata-panel` | Nuxt 4 + Nuxt UI, SPA, staff panel containing **both the RMS and the CRM** (header switch between them) | 3001 |
| `anakata-engine` | Nuxt 4 + Nuxt UI, SSR, public booking engine | 3000 |

## Where the requirements live
All requirements are in `anakata-api/docs/requirements/` (from other repos: `../anakata-api/docs/requirements/`).
Sprints are in `anakata-api/docs/sprints/sprint-NN/` — a `README.md` (context for the whole sprint) and ordered task files (`01-….md`, `02-….md`), handed to you **one at a time**. Read `docs/requirements/08-dev-decisions.md` first — it records architecture choices and resolves contradictions between the documents.

**Order of authority when documents disagree** (highest first):
1. `08-dev-decisions.md` (development decisions + resolved contradictions)
2. `05-decisions-and-open-questions.md` (Anakata's decisions of 12 Sep 2026)
3. `07-three-system-integration-contract.md` (field ownership, events)
4. `01`–`04` (functional spec, data model, business rules, engine contract)
5. The RMS and CRM prototypes (HTML) — both implemented in `anakata-panel` — behaviour and design reference
6. The booking-engine `booking_engine_SPEC.md` (with `booking_engine_README.md`) and its prototype `prototype/booking_engine_index.html` — **v3, written before the 12 Sep decisions**; its design is the reference, but its business rules lose to everything above.

`examples/seed-data.json` = fixtures. `examples/booking-engine-feed.json` = feed shape. `screenshots/` = visual acceptance reference.

## Non-negotiable rules
1. **Single write path.** Every field has one write path: every mutation is one Action. The CRM section never writes money or booking data. No read-only mirrors, no internal sync events.
2. **Server-side enforcement.** Every permission, status transition, price, discount, commission cap and availability check is enforced in `anakata-api`. The frontends are convenience only — never the source of truth.
3. **Rules are data, not code.** Rates, business rules and engine settings live in the database with their source code (FIN-001, OPS-004…). Never hard-code a business value (a price, a percentage, a number of days) anywhere. If you need one, read it from the rules/rates/settings store.
4. **Money** is USD integers (no cents) in the RMS and API. Cents only on rendered documents. Never floats.
5. **Dates** are ISO `YYYY-MM-DD`, stored UTC. Departures are Sundays; cruises are 7 nights, Sunday → Sunday.
6. **Append-only history.** Bookings, rates, rules, settings and agent actions keep a history of `at, actor, what, why`. Nothing is edited or deleted in history. Reason is mandatory where the docs say so.
7. **Frozen at sale.** A booking's price lines, rates and deposit % never change when rates change later.
8. **English only**, guest-facing and internal. Keep UI strings externalised (i18n files) so a second locale stays possible.
9. **No automation ever cancels a booking** (OPS-007).
10. **Passport and medical data** are encrypted at rest, restricted by role, never sent to the CRM.

## Never guess
- If a requirement is missing, ambiguous or contradicts another document and `08-dev-decisions.md` does not resolve it: **stop and ask**, or implement the smallest safe version and leave `// TODO(OPEN: <ref>) <question>` referencing the doc/open item (e.g. `LEG-001`, `B6`).
- Never invent business values, copy, enums or statuses. Enums come from doc 02 and the prototypes (e.g. `main_channel` has 8 values, `channel_of_origin` 40 — take them from the prototype source, do not make them up).

## Design fidelity
The design is decided: **the apps must look exactly like the prototypes** (`prototype/rms_index.html` = RMS screens of the panel, `prototype/crm_index.html` = CRM screens of the panel, `prototype/booking_engine_index.html` = engine). When building a screen, open the prototype's HTML/CSS for that screen and the matching screenshot, and match spacing, type, colour, borders and states. Square corners, hairline borders, uppercase mono labels, no shadows, no rounded Nuxt UI defaults. Check both dark (default) and light themes.

## How to work a task
1. You receive **one task file** at a time. Read the sprint's `README.md` first, then the task, then every document the task lists under "Read first" — before writing code.
2. Do **only that task**. Do not start the next task, even if it looks obvious. If something outside the task is needed, write it under "Notes for later" in the report instead of building it.
3. Write tests for every rule and calculation you implement.
4. The task's checks (lint, typecheck, tests) must pass before you say you are done. In anakata-api, run them inside Docker (see `laravel.mdc`).
5. Finish by **appending a section** to the sprint's `REPORT.md` (`anakata-api/docs/sprints/sprint-NN/REPORT.md`): task number and title, what was built, files touched, deviations from the task and why, open questions, notes for later.
